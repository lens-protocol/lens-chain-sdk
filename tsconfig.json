{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "lib": [
      "ESNext",
      "DOM" // Add "DOM" to avoid IDE errors on examples/
    ]
  },
  "include": ["**/*.ts"]
}
